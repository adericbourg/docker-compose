apiVersion: 1

providers:
  - name: default
    orgId: 1
    folder: Network
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
